# Deployment

Phase 3 — not yet written. Scope: Railway (`apps/api` + Postgres), Cloudflare Pages (`apps/web`), R2 (checkpoints and
action logs), optional Cloudflare Tunnel, and the laptop brain worker that connects outbound to the API.
Nothing described here exists in the repository yet.
