# Protocol

Phase 2–3 — not yet written. Scope: the typed WebSocket/HTTP message envelope `{type, v, ts, payload}` between the
brain worker, the API hub and browsers, defined once as JSON Schema in `packages/protocol` (TypeScript types and
Python pydantic models generated from the same source). Nothing described here exists in the repository yet.
