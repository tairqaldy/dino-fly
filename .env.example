# Copy to .env (git-ignored). Only variables that are actually read by code today are listed here;
# later phases (API, worker, R2, hardware) add theirs when that code lands.

# Where connectome / annotation files are downloaded to. Default: <repo>/data
DINOFLY_DATA_DIR=
