# Cloudflare Pages project for apps/web (static).   pnpm --filter @dino-fly/web build && npx wrangler pages deploy apps/web/dist --project-name dino-fly
name = "dino-fly"
pages_build_output_dir = "../apps/web/dist"
compatibility_date = "2026-09-01"
