{
  "name": "@dino-fly/protocol",
  "version": "0.1.0",
  "private": true,
  "description": "Shared realtime message schemas (JSON Schema = source of truth) and TypeScript types.",
  "license": "MIT",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json"
  },
  "devDependencies": {
    "@types/node": "^26.6.2",
    "ajv": "^8.17.1",
    "typescript": "^7.0.2",
    "vitest": "^5.0.1"
  }
}
