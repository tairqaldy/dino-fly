{
  "name": "@dino-fly/dino-core",
  "version": "0.1.0",
  "private": true,
  "description": "Deterministic, all-integer Chrome-Dino-style game engine. Source of truth for the Python port and the API replay validator.",
  "license": "MIT",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "test": "vitest run --coverage",
    "typecheck": "tsc -p tsconfig.json",
    "gen-fixtures": "tsx scripts/gen-fixtures.ts"
  },
  "devDependencies": {
    "@types/node": "^26.6.2",
    "@vitest/coverage-v8": "^5.0.1",
    "tsx": "^4.23.15",
    "typescript": "^7.0.2",
    "vitest": "^5.0.1"
  }
}
