# flybrain

Python side of dino-fly: connectome loader, our own Brian2-faithful LIF engine (PyTorch), fixed transducers,
the Python port of the deterministic game engine, and every experiment whose numbers appear in `docs/RESEARCH.md`.

See the repository root `README.md` and `CLAUDE.md` for commands and conventions.
